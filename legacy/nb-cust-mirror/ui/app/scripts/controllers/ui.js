'use strict';

angular.module('novaballApp')
    .controller('UICtrl', ['$scope', '$rootScope', 'Auth', 'Token',
        function ($scope, $rootScope, Auth, Token) {
            console.log("UICtrl: init");
        }]);
